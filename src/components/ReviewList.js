import React from "react";

export default function ReviewList(props) {

  return (

	<div>
      {/* Pull out code for this component from ReviewForm component.
	  Couldn't find a way of passing the reviews array to this component
	  from the ReviewForm component where all reviews are exist. */}

	</div>
  )
}