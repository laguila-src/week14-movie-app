import React from "react";

// This component was meant to be a text review but the review data
// is inside the ReviewForm component. Modify later.
const Review = () => {
	return (
		<div>
			{/* Holds the text in a single review */}
		</div>
	)
}

export default Review;